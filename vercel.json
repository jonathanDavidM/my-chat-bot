{
  "buildCommand": "node scripts/build-knowledge.mjs && npm run build && npm run build:embed && node scripts/copy-embed.mjs",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/chat/send.ts": { "maxDuration": 60 }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
